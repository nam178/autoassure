import { Container, Title } from "@mantine/core";
import { memo } from "react";

export const ScenarioPage = memo(function ScenarioPage() {
  return (
    <Container>
      <Title order={1}>Scenarios</Title>
    </Container>
  );
});
