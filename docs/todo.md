## Todos

1. Test the login feature end-to-end — the code was just implemented but hasn't actually been tested yet.
2. Auto-refresh auth tokens before they expire. If a refresh fails, sign the user out immediately so the UI can show a "you've been logged out" screen.
3. Implement the "create new test flow" feature — this is one of the core features and will teach us a lot about how AutoAssure should work.
