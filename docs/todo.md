## Todos

~~1. Should we introduce the concept of Agents?~~

I want to dive deeper into what execution plan looks like here. If it's a graph then I think it will scare the user away and it's also pretty hard to implement. Maybe it should just be a linear list. But if I use a linear list, you wouldn't be able to do branchings. Like if scenario this scenario produced this value then execute this branch. Also there are plans that require there are plans that require repetitive setup For example, a plan for testing the shopping cart, you might want to verify that payment succeed, payment faail shipping address invalid all that would require all that plan would require a new go to the website and adding items into the shopping cart. So that behavior that user adding items into the shopping cart show up repeatedly in multiple planes, how do we reduce the repetitiveness? 


1. Screens?
1. Observability and dashboards - where do they sit?
1. Should we add margin when billing customer the use of tokens? Should we allow
   customer to plugin their own open API key or bedrock.
1. How do we execute tests? Deploy test clients? What platforms should test
   clients be written for?
1. Color theme/black & white?
1. How to structure the app? Pages? Menus? Navigation?

```

```
