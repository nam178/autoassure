## Research topics

We still need to decide how screens should work across the product, i.e. what a "screen" is as a concept and how it fits alongside scenarios and test flows.

We haven't figured out where observability and dashboards sit in the product, i.e. whether they're part of the core app or a separate surface.

We need to decide whether to add margin when billing customers for token usage, and whether to let customers plug in their own OpenAI key or Bedrock credentials instead.

We need to figure out how tests actually get executed, including whether we deploy test clients and what platforms those test clients should be written for.

We haven't settled on a visual direction yet, i.e. whether the app uses a color theme or stays black and white.

We still need to work out how to structure the app overall, including its pages, menus, and navigation.
